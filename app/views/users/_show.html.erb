<div>
  <h3 class="user-name"> <strong> Name: </strong> <%= @user.full_name %> <strong>Email: </strong><%= @user.email  %></h3>
</div>
<div class="d-flex">
  <% @user.assignments.each do |assignment| %>
    <div class="assignment-cards">
      <%= assignment.task.title %>
    </div>
  <% end %>
</div>
