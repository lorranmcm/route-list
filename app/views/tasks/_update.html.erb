<%#
<div class="whitebck" data-edittask-target="taskdescription">
  <h1>to dos</h1>
  <%= @task.description%>
  <br>
  <a class="btn btn-warning" href="#">Edit</a>

  <tr id="test">
      <td class="input">
        <%= form_for @task, :method => :PATCH do |f| %>
        <%= f.text_field :title, :value => @task.title %>
      </td>
      <td class="input">
        <%= f.text_field :description %>
      </td>
      <td class="input">
        <%= f.text_field :address %>
      </td>
      <td class="grayedOut"></td>
      <td class="actions">
        <%= f.submit "Confirm edit" %>
        <% end %>
      </td>
  </tr>

  <%= simple_form_for([ task ],
                      data: {
                        insert_in_list_target: 'form',
                        action: 'submit->update#updatetask'
                      }) do |f| %>
    <%= f.input :content, as: :text %>
    <%= f.button :submit %>
  <% end %>
</div> %>
